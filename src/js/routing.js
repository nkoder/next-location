define(function () {

  function init() {
    console.log("routing here...");
    //var edison = new Edison({
    //  'container': 'dummy-route-container'
    //});
    //var locations = edison.createSection({
    //  'name': 'locations',
    //  'callback': function () {
    //  }
    //});
    //locations.createRoute({
    //  'name': '1',
    //  'callback': function () {
    //    var routePath = "#!" + this.section.route.name + "/" + path;
    //    $('#main-content').html('SDSDSDSD');
    //page.loadInto('#page-content', '#page-header', param);
    //menu.select(routePath);
    //}
    //});
  }

  return {
    init: init
  };
});
